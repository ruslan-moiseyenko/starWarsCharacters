import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const Main = () => {
  return (
    <View>
      <Text>Main</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
